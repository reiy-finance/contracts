{
  "name": "reiy-benchmark",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "doctor": "bun run src/doctor.ts",
    "register-solver": "bun run src/register-solver.ts",
    "intents": "bun run src/run-intents.ts",
    "settlements": "bun run src/run-settlements.ts",
    "e2e": "bun run src/run-e2e.ts",
    "figures": "python3 scripts/plot.py",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mysten/sui": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
